// Minimal example of using gpurhh.
//
// Currently a placeholder. The host side (construct, destruct, view) is
// implemented; the device-side View::insert and View::get are still TODO.
// Once they land, this file will demonstrate constructing a HashTable,
// performing a bulk insert, and looking the keys back up.

#include <gpurhh/hash_table.cuh>

int main() {
    return 0;
}
